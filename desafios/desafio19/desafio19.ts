const lista = ["maça", "banana", "uva", "laranja", "abacaxi"]

const indiceAleatorio = Math.floor(Math.random() * lista.length)

const itemAleatorio = lista[indiceAleatorio]

console.log("Item Sorteado", itemAleatorio)


